## API Gateway endpoints
# POST body to save into Aurora (via your API Gateway + Lambda/service)
VITE_SAVE_URL="https://example.execute-api.us-east-1.amazonaws.com/prod/save"

# GET to retrieve data from Aurora
VITE_RETRIEVE_URL="https://example.execute-api.us-east-1.amazonaws.com/prod/retrieve"

# GET to return the EC2 region (served by your backend/API; browser cannot call EC2 metadata directly)
VITE_REGION_URL="https://example.execute-api.us-east-1.amazonaws.com/prod/region"

## Contact
# This template intentionally does NOT include a contact form integration.
# Consider using:
# - a `mailto:` link
# - a hosted form service (Formspree, Basin, Getform)
# - or your own API endpoint behind API Gateway

